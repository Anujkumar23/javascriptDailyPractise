// String Conversion happen when we need a string from a value.



// we  can  call a String (value) to change a value from String


let value = true;
document.write(typeof value);

value = String(value);
document.write(typeof value)

